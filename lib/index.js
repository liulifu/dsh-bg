//#region lib/types/index.js
/** Host loader entry for the browser-only background plugin. */
/** Provides no host-side behavior; all logic lives in the client bundle. */
function apply() {}
//#endregion
export { apply };
